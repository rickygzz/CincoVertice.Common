# CincoVertice.Common

This repository is designed to be used as a git submodule and provides reusable components and utilities
built with .NET 9, enabling efficient and consistent development across projects.

To add this repository as a submodule to your project, use the following command:

```bash
git submodule add https://github.com/rickygzz/CincoVertice.Common.git common
```
